spring:
  application:
    name: yubao-live-user-provider # 服务名称
  profiles:
    active: dev # 当前使用dev环境
  cloud:
    nacos:
      config:
        server-addr: 117.72.52.137:8848
        username: yubao
        password: yubao
        file-extension: yaml
        group: DEFAULT_GROUP # 可选，默认是 DEFAULT_GROUP
        grpcTslConfig:
          enableTls: false
          trustAll: true
