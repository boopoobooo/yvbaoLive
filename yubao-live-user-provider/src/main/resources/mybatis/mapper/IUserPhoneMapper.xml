<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.junbao.yubao.live.user.provider.dao.mapper.IUserPhoneMapper">

    <resultMap id="dataMap" type="cn.junbao.yubao.live.user.provider.dao.po.UserPhonePO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    <insert id="insertOne" parameterType="cn.junbao.yubao.live.user.provider.dao.po.UserPhonePO">
        INSERT INTO user_phone (
            user_id,phone,status,create_time,update_time
        )
        VALUES (
                   #{userId},#{phone}, #{status}, now(),now()
               )
    </insert>
    <select id="queryByPhone" resultMap="dataMap">
        select * from user_phone
        where phone = #{phone}
    </select>
    <select id="queryByUserId" resultMap="dataMap">
        select * from user_phone
        where user_id = #{userId}
    </select>


</mapper>
