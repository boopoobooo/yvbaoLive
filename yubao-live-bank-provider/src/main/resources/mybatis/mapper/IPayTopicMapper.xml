<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.junbao.yubao.live.bank.provider.dao.mapper.IPayTopicMapper">

    <!-- 定义结果映射 -->
    <resultMap id="dataMap" type="cn.junbao.yubao.live.bank.provider.dao.po.PayTopicPO">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="topic" property="topic" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="biz_code" property="bizCode" jdbcType="INTEGER"/>
        <result column="remark" property="remark" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>


    <select id="selectOne" resultType="cn.junbao.yubao.live.bank.provider.dao.po.PayTopicPO">
        SELECT *
        FROM
            pay_topic
        WHERE
            biz_code = #{bizCode} and
            status = #{status}
        LIMIT 1
    </select>

    <!-- 插入语句 -->
    <insert id="insertOne" parameterType="cn.junbao.yubao.live.bank.provider.dao.po.PayTopicPO">
        INSERT INTO pay_topic
            (id, topic, status, biz_code, remark, create_time, update_time)
        VALUES
            (#{id,jdbcType=BIGINT}, #{topic,jdbcType=VARCHAR}, #{status,jdbcType=INTEGER},
             #{bizCode,jdbcType=INTEGER}, #{remark,jdbcType=VARCHAR}, #{createTime,jdbcType=TIMESTAMP},
             #{updateTime,jdbcType=TIMESTAMP})
    </insert>

    <!-- 更新语句 -->
    <update id="update" parameterType="cn.junbao.yubao.live.bank.provider.dao.po.PayTopicPO">
        UPDATE pay_topic
        SET
            topic = #{topic,jdbcType=VARCHAR},
            status = #{status,jdbcType=INTEGER},
            biz_code = #{bizCode,jdbcType=INTEGER},
            remark = #{remark,jdbcType=VARCHAR},
            create_time = #{createTime,jdbcType=TIMESTAMP},
            update_time = #{updateTime,jdbcType=TIMESTAMP}
        WHERE
            id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 删除语句 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM pay_topic
        WHERE
            id = #{id,jdbcType=BIGINT}
    </delete>


</mapper>
