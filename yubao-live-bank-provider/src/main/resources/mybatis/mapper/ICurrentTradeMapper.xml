<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.junbao.yubao.live.bank.provider.dao.mapper.ICurrencyTradeMapper">

    <resultMap id="dataMap" type="cn.junbao.yubao.live.bank.provider.dao.po.CurrencyTradePO">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="num" column="num"/>
        <result property="type" column="type"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="insertOne" parameterType="cn.junbao.yubao.live.bank.provider.dao.po.CurrencyTradePO">
        INSERT INTO currency_trade (user_id, num, type, status, create_time, update_time)
        VALUES (#{userId}, #{num}, #{type}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectById" parameterType="long" resultMap="dataMap">
        SELECT * FROM currency_trade WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="dataMap">
        SELECT * FROM currency_trade
    </select>


</mapper>
