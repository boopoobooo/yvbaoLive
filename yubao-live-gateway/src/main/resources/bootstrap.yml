server:
  port: 80
spring:
  application:
    name: yubao-live-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 117.72.52.137:8848
        username: yubao
        password: yubao
      config:
        import-check:
          enabled: false
        # 当前服务启动后去nacos中读取配置文件的后缀
        file-extension: yaml
        # 读取配置的nacos地址
        server-addr: 117.72.52.137:8848
        username: yubao
        password: yubao
  config:
    import:
      - optional:nacos:yubao-live-gateway.yaml

# 日志
logging:
  level:
    root: info
  config: classpath:logback-spring.xml
